<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.5</version>
        <relativePath/>
    </parent>

    <groupId>com.bravapro</groupId>
    <artifactId>brava-backend</artifactId>
    <version>1.0.0</version>
    <name>brava-backend</name>
    <description>Sistema de gestão para negócios da área da beleza — parent POM</description>
    <packaging>pom</packaging>

    <modules>
        <module>brava-core</module>
        <module>brava-clients</module>
        <module>brava-services</module>
        <module>brava-appointments</module>
        <module>brava-finance</module>
        <module>brava-stock</module>
        <module>brava-fichas</module>
        <module>brava-dashboard</module>
        <module>brava-settings</module>
        <module>brava-app</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <jjwt.version>0.12.6</jjwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Módulos internos -->
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-clients</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-services</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-appointments</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-finance</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-stock</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-fichas</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-dashboard</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bravapro</groupId>
                <artifactId>brava-settings</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <!-- Necessário pro Jackson desserializar os *Command (Lombok, sem record —
                             record não pode estender AbstractCommand) direto via @RequestBody,
                             usando os nomes reais dos parâmetros do construtor (ParameterNamesModule). -->
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <skip>true</skip>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!-- Lint de qualidade de código Java.
                 mvn pmd:check -> falha se encontrar violação (método longo, código morto, etc)
                 mvn pmd:pmd   -> gera o relatório em target/site/pmd.html -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>3.25.0</version>
                <configuration>
                    <rulesets>
                        <ruleset>rulesets/java/quickstart.xml</ruleset>
                    </rulesets>
                    <linkXRef>false</linkXRef>
                    <failOnViolation>true</failOnViolation>
                    <printFailingErrors>true</printFailingErrors>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
